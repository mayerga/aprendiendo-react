import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

//El main es el punto de entrada a la aplicación. Desde donde se construye la apliación
//Es el primer archivo que se carga para cargar la aplicación